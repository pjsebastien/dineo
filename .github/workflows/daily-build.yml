name: Daily Publish

on:
  schedule:
    - cron: '0 5 * * *'
  workflow_dispatch:

jobs:
  trigger-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: curl -X POST "https://api.vercel.com/v1/integrations/deploy/prj_idkMTiJr0bHI5eYhCK8yDGykJcwo/KMKrHiBQiL"

      - name: Log
        run: |
          echo "✅ Deploy hook triggered"
          echo "📅 Date: $(date)"
